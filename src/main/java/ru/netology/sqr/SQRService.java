package ru.netology.sqr;

public class SQRService {

    public int calculateAmountSquare(int rangeLimit1, int rangelimit2) {

        for (int i = 10; i <= 99; i++) {
            if (counter >= rangeLimit1 && counter <= rangelimit2) ;
            counter++; {
                return counter;
            }
        }
            return 0;
        }
    }
